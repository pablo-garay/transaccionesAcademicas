
Sistema de Transacciones Académicas - UCA

Usted ha recibido este email porque solicitó restablecer su contraseña con nosotros. Para continuar con el proceso, por favor presione el enlace de abajo.

<?php echo $this->serverUrl($this->url('zfcuser/resetpassword', array('userId' => $this->record->getUserId(), 'token' => $this->record->getRequestKey()))); ?>